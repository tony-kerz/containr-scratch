{
  "name": "containr-scratch",
  "version": "1.0.0",
  "exports": "./src/index.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "watch": "node --env-file=.env --watch-path . src/index.js",
    "pretest": "npx xo",
    "test": "DEBUG=dbg:* NODE_ENV=test npx ava --serial",
    "posttest": "npm run clear",
    "dev-container": "docker compose up --build",
    "pretest-container": "docker build -t $(npm run tag-sha --silent) --progress=plain --target=test .",
    "test-container": "docker run -v /var/run/docker.sock:/var/run/docker.sock -v /tmp:/tmp -u $(npm run user --silent) $(npm run tag-sha --silent)",
    "posttest-container": "npm run clear",
    "lock": "docker build -t $(npm run tag --silent) --target lock --output type=local,dest=. .",
    "build-container": "docker build -t $(npm run tag --silent) --progress=plain --target=prod .",
    "run-container": "docker run -v /var/run/docker.sock:/var/run/docker.sock -v /tmp:/tmp -u $(npm run user --silent) -e OCI_IMAGE=ttl.sh/oci-scratch:1.0.0 -e IS_LOCAL=1 -e DEBUG=dbg:* $(npm run tag --silent)",
    "postrun-container": "npm run clear",
    "tag": "echo ${npm_package_name}:${npm_package_version}",
    "tag-sha": "echo ${npm_package_name}:$(git rev-parse --short HEAD^)",
    "tag-full": "echo ${npm_package_name}:${npm_package_version}-$(git rev-parse --short HEAD^)",
    "user": "echo $(id -u)",
    "clear": "rm -rf /tmp/containr"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@watchmen/containr": "^1.0.6",
    "@watchmen/debug": "^2.0.8",
    "@watchmen/helpr": "^3.0.0",
    "config": "^3.3.12",
    "debug": "^4.4.0",
    "execa": "^9.5.2",
    "fs-extra": "^11.2.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "ava": "^6.2.0",
    "prettier": "^3.4.2",
    "xo": "^0.60.0"
  }
}
